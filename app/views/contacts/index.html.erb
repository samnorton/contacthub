<%= render 'layouts/searchbar' %>
<!-- Main Content Start -->
  <section id="main-content">
    <div class="container">
      <div class="row">
        <%= render 'layouts/sidebar_nav' %>
        <div class="col-lg-8">
          <div class="card">
            <div class="card-header">
              <strong class="float-left mt-1">Contact List</strong> <span><button class="btn btn-primary border-button float-right" data-target="#new-contact-modal" data-toggle="modal"><span><i class="fa fa-plus white"></i> Add New</span></button></span>
            </div>
            <%= render @contact %>
            <%= render 'layouts/pagination' %>
          </div>
        </div>
      </div>
    </div>
  </section>
<!-- Main Content End -->
